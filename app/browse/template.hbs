<nav>
  <input
    data-t="nav-search"
    oninput={{perform handleSearchInput}}
    type="text"
    value={{query}}
  />
</nav>

{{#each model.podcasts as |podcast|}}
  <p data-t="podcast">
    {{link-to podcast.title "podcast" podcast.id}}
  </p>
{{/each}}
